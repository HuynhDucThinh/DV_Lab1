# Copy this file to .env and fill in the key(s) for the provider(s) you want to use.
# Only the providers with keys set will appear as options in the dashboard.
# Priority order when auto-selecting: openai → gemini → claude → grok

OPENAI_API_KEY=
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
GROK_API_KEY=
GROQ_API_KEY=
