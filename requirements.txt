# ============================================================
# requirements.txt — Streamlit Cloud deployment
# Chỉ bao gồm packages cần thiết cho dashboard/app.py
# Để dev local + notebooks: pip install -r requirements-dev.txt
# ============================================================

# Core data
pandas>=2.2.3
numpy>=2.1.0

# Visualization (dashboard dùng Plotly)
plotly>=5.19.0

# Dashboard
streamlit>=1.40.0
streamlit-shadcn-ui==0.1.19

# Machine Learning inference (chạy model đã train sẵn)
scikit-learn>=1.6.0
xgboost>=2.1.0
joblib>=1.4.0

# LLM Explainer — optional, cần API key trong Streamlit Secrets
openai>=1.0.0
google-generativeai>=0.5.0
anthropic>=0.20.0

# Utilities
python-dotenv>=1.0.1
