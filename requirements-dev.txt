# ============================================================
# requirements-dev.txt — Full local development environment
# Dùng để cài local: pip install -r requirements-dev.txt
# ============================================================

# Bao gồm tất cả packages production
-r requirements.txt

# Thu thập dữ liệu (không hỗ trợ trên Cloud)
selenium==4.18.1
playwright==1.42.0

# Machine Learning mở rộng
lightgbm==4.3.0

# NLP tiếng Việt (nặng, không hỗ trợ trên Cloud)
underthesea==6.8.0

# Các framework dashboard khác (để thử nghiệm local)
dash==2.16.1
panel==1.3.8
